<h2>Edit event</h2>

<%= render "form.html", changeset: @changeset,
                        action: event_path(@conn, :update, @event.id, @event) %>

<%= link "Back", to: event_path(@conn, :index, @organization) %>
